# Segmentation Integration

AeroRescue-AI uses semantic segmentation classes for disaster-scene environmental understanding. The segmentation layer helps convert an image from simple target detection into a richer rescue decision input.

## Current Implemented

- Uploaded mask parsing
- Mask validation for class ids, unknown ids, and fallback behavior
- RGB mask and class-id mask support
- Palette-mode mask conversion through RGB color mapping
- Segmentation overlay visualization
- Area summary for environmental classes
- Environment risk fusion for detected targets
- A* path cost map generation from segmentation classes

## Experimental

Optional automatic segmentation model inference is available if a trained local checkpoint exists.

Supported checkpoint locations:

```text
checkpoints/segmentation_model.pth
app/segmentation_weights/segmentation_model.pth
```

If no checkpoint is found, the app does not generate a fake segmentation mask. It shows a clear status message and falls back to no segmentation mask.

## Smoke Test

Run the core smoke test without training, downloading data, loading YOLO weights, or starting Gradio:

```bash
python tests/smoke_test_core.py
```

The test covers segmentation mask validation, segmentation summary, overlay creation, path cost map generation, A* path planning, and missing-checkpoint fallback.

## Not Included By Default

- Full segmentation dataset
- Large pretrained segmentation checkpoint
- Official training results

## Image Policy

- Do not copy external README images into this repository.
- Do not use external image links in the demo gallery.
- Do not present external images as AeroRescue-AI generated outputs.
- Use local project assets or outputs generated by this project whenever possible.

## Future Work

- Train and evaluate a lightweight segmentation model on local disaster-scene image/mask data.
- Report pixel accuracy, mean IoU, and per-class IoU.
- Replace manual mask upload with validated automatic segmentation inference when a real checkpoint is available.
- Improve path planning with stronger passability constraints and multi-target route ordering.
