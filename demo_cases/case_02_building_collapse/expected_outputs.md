# Expected Outputs

This case should show how `major_damage` and `destroyed_building` increase environment risk and path cost.

Expected generated artifacts match the standard showcase structure under `static/images/showcase/case_02_building_collapse/`.
